> Updates soon, currently the auth in HostIMG is being developed and tested, because of that only the start of these services is marked, with a telegram bot in python and a website talking to hostimg's core api and sharing the same user's database. This repository is deliberately public and separate from the actual HostIMG repo to show that it is a completely separate thing from the interfaces it talks to - it just provides the backend and the API to build an application